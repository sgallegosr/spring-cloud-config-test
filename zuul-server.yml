server:
  port: 8080 #the default port used for zuul service
  
zuul:
  ignoredServices: '*'
  routes:
    student-service: 
      path: /student/** 
      stripPrefix: false  
      sensitiveHeaders:
      url: http://localhost:8098